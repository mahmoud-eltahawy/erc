import LoginForm from './LoginForm'
import ShiftIdentity from './ShiftIdentity'

function App() {
  return (
      <section>
        <ShiftIdentity/>
        <LoginForm/>
      </section>
  );
}

export default App;
